[project]
name = "playground"
version = "0.1.0"
description = "Playground repository for miscellaneous scripts"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
keep-awake = "keep_awake:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
only-include = ["keep_awake.py"]
